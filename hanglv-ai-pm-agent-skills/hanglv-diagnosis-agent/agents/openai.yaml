interface:
  display_name: "航旅诊断 Agent"
  short_description: "判断问题真假、根因、产品可解性和优先级"
  default_prompt: "Use $hanglv-diagnosis-agent to diagnose a Hanglv flight-ticket problem and decide whether it should enter solution design."
policy:
  allow_implicit_invocation: true
